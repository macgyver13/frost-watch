baseURL = "https://frost-watch.macgyver-dev.workers.dev/"
locale = "en-us"
title = "FROST Watch"
enableRobotsTXT = true
disablePathToLower = true

[params]
  description = "Public-source activity tracking for FROST and directly related dependencies."
  serving_mode = "service"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
